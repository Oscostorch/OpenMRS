version: '3.8'
services:
  postgres:
    image: postgres:14
    environment:
      POSTGRES_USER: openmrs
      POSTGRES_PASSWORD: openmrs
      POSTGRES_DB: openmrs_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
